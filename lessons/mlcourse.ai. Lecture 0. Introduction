- completed https://www.youtube.com/watch?v=QKTuw4PNOsU&list=PLVlY_7IJCMJeRfZ68eVfEcu-UcN9BbwiX
- Articles - https://mlcourse.ai
- videos https://mlcourse.ai/lectures
- prerequest - https://mlcourse.ai/prerequisites
- kaggle https://www.kaggle.com/kashnitsky/mlcourse
- 

np.array([1,3,4,5])
sum, .median

pd.series 
+index 
+name


dataframe 
many series object 
age salary debt 
can get pd.series from tht data frame 
df["salary"]
type(df["salary"])

- difference between np.ndarray- is a structure and np.array() - is a function


# A structured array
my_array = np.ones(3, dtype=([('foo', int), ('bar', float)]))
print(my_array['foo'])

# A record array
my_array2 = my_array.view(np.recarray)
print(my_array2.foo)

https://www.datacamp.com/community/tutorials/pandas-tutorial-dataframe-python?utm_source=adwords_ppc&utm_campaignid=1455363063&utm_adgroupid=65083631748&utm_device=c&utm_keyword=&utm_matchtype=b&utm_network=g&utm_adpostion=1t1&utm_creative=278443377095&utm_targetid=aud-748597547652:dsa-473406573835&utm_loc_interest_ms=&utm_loc_
physical_ms=1007772&gclid=CjwKCAjwtuLrBRAlEiwAPVcZBoN3SrRnm179zy6sFhct1Ak768cRJctz44MbdTcc6finuY3iKJrswBoCDN0QAvD_BwE

You could also use df[0].count() to get to know more about the height of your DataFrame, 
but this will exclude the NaN values (if there are any). That is why calling .count() on your 
DataFrame is not always the better option.
list(my_dataframe.columns.values)
 A  B  C
0  1  2  3
1  4  5  6
2  7  8  9

And you want to access the value that is at index 0, in column ‘A’.

# Using `iloc[]`
print(df.iloc[0][0])

# Using `loc[]`
print(df.loc[0]['A'])

# Using `at[]`
print(df.at[0,'A'])

# Using `iat[]`
print(df.iat[0,0])

# Use `iloc[]` to select row `0`
print(df.iloc[_])

# Use `loc[]` to select column `'A'`
print(df.loc[:,'_'])
